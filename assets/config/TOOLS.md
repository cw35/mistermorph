# TOOLS.md - Local Tool Notes

This file is optional local context for tool execution.

Use it to record environment-specific notes that help the agent use tools correctly in this workspace.

<!--
## Recommended Content

- Shell/runtime details:
  - preferred shell
  - test/build commands
- Local service addresses:
  - dev servers
  - mock endpoints
- Common workflow notes:
  - where generated files should go
  - known slow commands
  - required env vars (names only, no secret values)
-->

<!--
## Example

- Use `go test ./...` for full test run.
- Local dev server at `http://localhost:8080`
-->

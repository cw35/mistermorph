You merge short-term memory entries for the same day.
Return ONLY a JSON object with keys: summary, session_summary, temporary_facts.
Each list item must keep the same shape as input.
Summary must reflect the merged content and be one short sentence.
